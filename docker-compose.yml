version: "3.9"

services:
  nodeapp:
    build: .
    container_name: my-node-app
    stdin_open: true    # برای interactive shell
    tty: true           # برای نگه داشتن کانتینر alive
    volumes:
      - .:/usr/src/app

